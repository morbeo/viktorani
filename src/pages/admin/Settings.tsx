import AdminLayout from '@/components/AdminLayout'

export default function Settings() {
  return (
    <AdminLayout title="Settings">
      <p style={{ color: 'var(--color-muted)' }}>Settings — coming soon.</p>
    </AdminLayout>
  )
}
