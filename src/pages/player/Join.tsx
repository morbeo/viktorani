export default function Join() {
  return (
    <div className="flex items-center justify-center h-screen" style={{ background: 'var(--color-cream)' }}>
      <p style={{ color: 'var(--color-muted)' }}>Join — coming soon.</p>
    </div>
  )
}
